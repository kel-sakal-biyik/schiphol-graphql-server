type Flight {
    flightName: String
    scheduleTime: String
    airlineConnection: Airline
    destinationConnection: Destination
}

type Query {
    flights(scheduleDate: String, scheduleTime: String): [Flight]
}
